import { FC } from "react";
import { observer } from "mobx-react-lite";
import { Paper } from "@mui/material";

export const RenderFilterOption: FC<{}> = observer(({}) => {
    return <Paper>
        
    </Paper>
});